# =====================================================
# OpenAI configuration
# =====================================================
OPENAI_API_KEY=XXXXXXXXXXXXXXXXXX
OPENAI_MODEL=XXXXXXXXXXXXXXXXXX

# =====================================================
# CORS configuration
# =====================================================
# Use "*" to allow any origin during development.
# For production, replace with your domain, e.g.:
# CORS_ALLOW_ORIGINS=https://yourdomain.com
CORS_ALLOW_ORIGINS=*

# =====================================================
# Default system prompt
# =====================================================
DEFAULT_SYSTEM_PROMPT=XXXXXXXXXXXXXXXXXX
# =====================================================
# Google Cloud Storage configuration
# =====================================================
# The bucket name where conversation threads will be saved
GCS_BUCKET_NAME=XXXXXXXXXXXXXXXXXX
# Optional: change if you want a custom folder name for JSON threads inside the bucket
# GCS_THREADS_PREFIX=threads

# Optionally specify project ID if needed
# GOOGLE_CLOUD_PROJECT=my-gcp-project-id
